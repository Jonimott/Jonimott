digo algo de la ultma cosa, despues suelta la ppt

git soporta max 50mb por archivo

cada vez que se añaden archivos al repositorio van a quedar en staginf area  es como el check point
luego su comit loco 

mi carpeta-----se añade---se sube

traer siempre las ultimas versiones con el pull origin master
(buenas practicas) y luego hacer el push para guardar los cambios

esta el gitignore para ingorar cosa, primero se crea el archivo y se debe añadir con el commit incluido y su push
(son cosas que no quiero subir), se pueden poner patrones

comandos avanzadosgit branch nombre_rama, crea una nueva rama pero no se cambia a ella

git checkout nombre_rama para cambarme a la rama, cambia el HEAD

git push -u origin nombre_rama para llevar la rama a la maestraza